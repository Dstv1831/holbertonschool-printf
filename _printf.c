#include <stdard.h>
#include "main.h"

/**
 * _printf - Fucntion that produces the "printf"
 * output, according to a format
 *
 * @format: Character string
 *
 * Return: 0 Succes
 *
 */

int _printf(const char *format, ...)
{
	int i = 0, str_len = 0;
	va_list  args;

	va_start(args, format);


}
