#include "main.h"

/**
 * _printf - function that produces output according to a format.
 * @format: pointer format pointing to char value.
 *
 * Return:
 */
int _printf(const char *format, ...)
{
  return ();
}
